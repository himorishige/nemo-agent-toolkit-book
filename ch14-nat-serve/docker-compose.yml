services:
  nat-api:
    image: nat-nim-handson:1.6.0
    env_file:
      - .env
    ports:
      - '8000:8000'
    volumes:
      - ./workflow.yml:/app/workflows/workflow.yml:ro
    command:
      - 'serve'
      - '--config_file'
      - '/app/workflows/workflow.yml'
      - '--host'
      - '0.0.0.0'
      - '--port'
      - '8000'
