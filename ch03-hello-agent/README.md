# 第 3 章：最短 Hello Agent

`docker compose run nat` で `_type: nim` の ReAct エージェントを 1 回実行します。

## 動かし方

```bash
cp .env.example .env
# .env に NGC_API_KEY=nvapi-... を記入

docker compose run --rm nat
```

（執筆中）本章の詳細は Zenn Book の第 3 章を参照してください。
